image: node:20

pages:
  stage: deploy
  script:
    - npm ci
    - npm run build
    - mkdir -p public
    - cp -r dist/* public/   # <- copy contents, not folder
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH